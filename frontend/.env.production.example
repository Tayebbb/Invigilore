# Rename/copy to `.env.production` if you want production API URL baked into `vite build` locally.
# On Vercel, prefer setting VITE_API_BASE_URL in the project Environment Variables instead.

VITE_API_BASE_URL=https://invigilore.onrender.com/api
